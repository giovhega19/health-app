# Tareas · F04 Programación y recordatorios

Leyenda:

- `[P]` = paralelizable.
- Estado: ☐ pendiente · ◐ en curso · ☑ hecha.
- Cada tarea debe cumplir la DoD de `07-estrategia-pruebas.md`.
- `[CRUZADA]` marca una dependencia con una tarea de `specs/F03-editor-rutinas/tasks.md`.

> **Importante:** ninguna tarea de implementación empieza sin `F03-T00` (aprobación conjunta de H2, compartida con F03). La mayoría de las tareas de F04 corren en paralelo con las de F03 usando fixtures propias (`aRoutineSummary()`); la única tarea de F04 con una dependencia de entrada real desde F03 es `F04-T09`, que depende de `F03-T13`. `F04-T10` (backend) depende de que el dueño de producto confirme la pregunta abierta de `spec.md` (misma confirmación que cubre a F03).

| # | Estado | Tarea | Capa | Agente | CA cubiertos | Depende de |
|---|---|---|---|---|---|---|
| F04-T01 | ☑ | Redactar ADR-010 "Ventana móvil de 7 días para notificaciones locales, con replanificación al abrir la app" (`docs/adr/`, plantilla `templates/adr-template.md`) | — | arquitecto | — | F03-T00 |
| F04-T02 | ☑ [P] | Instalar `expo-notifications`; añadir plugin y configuración a `app.json` (icono/color Android); cambiar `app.json.expo.scheme` de `"mobile"` a `"fitapp"` (plan §1 "Corrección de configuración") | infrastructure | dev-mobile-rn | — | F03-T00 |
| F04-T03 | ☑ [P] | Contract-first: extender `SyncEntity` en `openapi.yaml` con `scheduleSlot` (coordinar con `F03-T03`, misma edición de archivo si es posible) y regenerar el cliente móvil | infra | dev-backend-java + dev-mobile-rn | — | F03-T00 |
| F04-T04 | ☑ | Pruebas de dominio para `NotificationPlanner` (tabla de casos: horas de silencio incl. cruce de medianoche, límite diario con exención RN-16, ventana de 7 días) y `RestRuleChecker` (RN-13, con `lastSessionInfo` inyectado) | domain | qa-pruebas | CA-04.02.1, CA-04.03.1, CA-04.04.1, CA-04.04.2, CA-04.06.1, CA-04.06.2 | F04-T01 |
| F04-T05 | ☑ | Implementar `domain/ScheduleSlot.ts`, `domain/SchedulingPreferences.ts`, `domain/PostponeCounter.ts`, `domain/deepLink.ts`, `domain/events.ts` | domain | dev-mobile-rn | CA-04.01.1, — | F04-T04 |
| F04-T06 | ☑ | Implementar `domain/NotificationPlanner.ts` (ventana de 7 días, horas de silencio, límite diario, prioridad) y `domain/RestRuleChecker.ts` (RN-13) | domain | dev-mobile-rn | CA-04.02.1, CA-04.03.1, CA-04.04.1, CA-04.04.2, CA-04.06.1, CA-04.06.2 | F04-T04, F04-T05 |
| F04-T07 | ☑ | Móvil: ampliar `shared/infrastructure/db/schema.ts` (`schedule_slots`, `planned_notifications`, `scheduling_preferences`, `postpone_counters`) + `infrastructure/Sqlite{ScheduleSlot,PlannedNotification,SchedulingPreferences,PostponeCounter}Repository.ts`, con pruebas de integración sobre SQLite real | infrastructure | dev-mobile-rn | — | F04-T06 |
| F04-T08 | ◐ | Casos de uso `ScheduleRoutine`, `UpdateSlot`, `CancelSlot`, `ReplanNotificationWindow`, `PostponeNotification`, `SkipToday`, `RequestNotificationPermission`, `UpdateSchedulingPreferences` (con `FakeNotificationScheduler`, nunca el SO real) + `infrastructure/NullLastSessionAdapter.ts` (*stub* de H2, plan §1) — hechos: `ScheduleRoutine`, `ReplanNotificationWindow`, `PostponeNotification`, `RequestNotificationPermission`, `NullLastSessionAdapter.ts`, más `ListActiveSlots` (nuevo, no listado originalmente, para el calendario semanal mínimo), y ahora también `CancelSlot` (cierre de brecha H2-QA: el agregado `ScheduleSlot.cancel()` y el suscriptor de `SlotCancelled` en `ReplanNotificationWindow`/`composition/container.ts` ya existían, pero ningún caso de uso de producción publicaba el evento — solo `ScheduleSlotSyncEntityApplier` al aplicar un `delete` remoto; `CancelSlot` persiste el borrado lógico vía `scheduleSlotRepository.cancel()` y publica `SlotCancelled` de verdad, cableado en `composition/container.ts` y con un punto de entrada UI mínimo, el botón "Cancelar" de `WeeklyCalendarScreen`, `F04-T13`). CA-04.01.2 verificado de punta a punta con `composition/__tests__/container.test.ts` (SQLite real: cancelar con el caso de uso real dispara `ReplanNotificationWindow`, que cancela en el SO las notificaciones ya programadas del slot — no solo construyendo el evento a mano). **Pendientes**: `UpdateSlot`, `SkipToday`, `UpdateSchedulingPreferences` (sin CA que los exija en H2; ver informe de cierre) | application | dev-mobile-rn | CA-04.01.1, CA-04.01.2, CA-04.01.3, CA-04.04.1, CA-04.04.2, CA-04.05.1 | F04-T07 |
| F04-T09 | ☑ **[CRUZADA]** | Caso de uso `CreateScheduleSlotsFromProposal` (suscriptor de `RoutinesCreatedFromProposal`) + wiring en `composition/container.ts` — cierra CA-02.04.3 de punta a punta (plan §1). Cierre de brecha H2-QA (RN-13, CA-04.04.1/CA-04.04.2): este era el único flujo real que creaba `ScheduleSlot`s sin pasar por `RestRuleChecker` (`ScheduleRoutine`, la programación manual, sí lo evaluaba). Ahora, por cada lote de slots que crea a partir de una propuesta, invoca `checkRestRules` (dominio) directamente comparando los slots del propio lote entre sí — la única forma de que la advertencia sea alcanzable de verdad en H2 sin sesiones completadas reales (`LastSessionQueryPort` sigue siendo un stub, `spec.md` §"Preguntas abiertas") — y dejas las advertencias en el nuevo puerto `RestWarningStore` (`InMemoryRestWarningStore`), que `WeeklyCalendarScreen` lee para mostrar un aviso junto al slot con descanso insuficiente (`F04-T13`) | application | dev-mobile-rn | CA-02.04.3 (cierre), CA-04.04.1 (cierre parcial vía lote), CA-04.04.2 (cierre parcial vía lote) | F03-T13, F04-T08 |
| F04-T10 | ☑ | Backend módulo `training` (ampliación, mismo módulo que `F03-T14`): `domain/ScheduleSlot.java`, puerto out `ScheduleSlotRepository`, persistencia JPA, migración Flyway `training/V2__create_schedule_slot_table.sql`, `ScheduleSlotSyncEntityHandler.java` + `infrastructure/ScheduleSlotSyncEntityApplier.ts` en el móvil | domain/infrastructure | dev-backend-java | — | F03-T14, F04-T03. **Bloqueada por confirmación del dueño de producto (spec.md §"Preguntas abiertas")** |
| F04-T11 | ☑ [P] | Instalar `expo-notifications` real (si no se hizo ya en `F04-T02`) + implementar `infrastructure/ExpoNotificationScheduler.ts` (adaptador real, canales Android `reminders`/`workout-timer`/`motivation`) | infrastructure | dev-mobile-rn | CA-04.01.3 | F04-T02, F04-T08 |
| F04-T12 | ☑ [P] | `RoutineSummaryLookupPort` + implementación en `composition/container.ts` que delega en `routines.getRoutineDetail` + `estimateRoutineDurationSeconds` (sin importar internos de `features/routines`) | application | dev-mobile-rn | CA-04.03.1 | F03-T09 (F03), F04-T08 |
| F04-T13 | ◐ | Pantallas: `WeeklyCalendar.tsx`, `SlotEditor.tsx` (muestra advertencia RN-13 no bloqueante), `NotificationSettings.tsx`, `PermissionDeniedBanner.tsx` (CA-04.01.3, enlaza a ajustes del sistema); `SessionDeepLinkPlaceholder.tsx` + ruta `app/session/start.tsx` (placeholder de F05, plan §1) — hechos: `WeeklyCalendar.tsx`, `PermissionDeniedBanner.tsx`, `SessionDeepLinkPlaceholder.tsx` + ruta, `SchedulingNavigator.tsx`, ruta `app/schedule/index.tsx`, con pruebas RNTL. `WeeklyCalendar.tsx` ahora además: (a) un botón "Cancelar" sobre cada slot programado (CA-04.01.2, cierre de brecha H2-QA — punto de entrada UI mínimo para `CancelSlot`, sin editor completo) y (b) un aviso junto al slot cuando `CreateScheduleSlotsFromProposal` dejó una advertencia de descanso en `RestWarningStore` (RN-13, CA-04.04.1/CA-04.04.2, cierre de brecha H2-QA); ambos verificados de punta a punta en `SchedulingNavigator.test.tsx` (navegador + pantallas + casos de uso reales, infraestructura en memoria) y — para "Cancelar" — también en `composition/__tests__/container.test.ts` con SQLite real. **Pendientes** (fuera de alcance de esta ronda, ver informe de cierre): `SlotEditor.tsx` completo (editar días/hora/aviso previo de un `ScheduleSlot` desde la UI — hoy solo se crea vía el evento `RoutinesCreatedFromProposal` o `ScheduleRoutine`, y solo se cancela, no se edita), `NotificationSettings.tsx` | presentation | dev-mobile-rn + ux-motivacion | CA-04.01.1, CA-04.01.2, CA-04.01.3, CA-04.04.1, CA-04.04.2 | F04-T08, F04-T12 |
| F04-T14 | ☐ [P] | Revisión de seguridad: confirmar que `schedule_slots`/`scheduling_preferences` no exponen datos de salud sensibles al backend más allá de lo ya cubierto por HTTPS+JWT; revisar que `DeleteAccount` (F01) alcanza también `training_schedule_slots` en el backend | — | seguridad-privacidad | — | F04-T10 |
| F04-T15 | ☐ | Verificación final y matriz de trazabilidad CA-04.01.1 … CA-04.06.2 (incluye CA-02.04.3 cierre) contra `plan.md` §5 | — | qa-pruebas | todos | F04-T09, F04-T11, F04-T13, F04-T14 |

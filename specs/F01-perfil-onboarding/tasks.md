# Tareas · F01 Cuenta, perfil y onboarding

Leyenda:

- `[P]` = paralelizable.
- Estado: ☐ pendiente · ◐ en curso · ☑ hecha.
- Cada tarea debe cumplir la DoD de `07-estrategia-pruebas.md`.
- `[CRUZADA]` marca una dependencia con una tarea de `specs/F02-catalogo-propuesta/tasks.md`.

> **Importante:** ninguna tarea de implementación (`F01-T01` en adelante) empieza sin `F01-T00` (aprobación del usuario de `plan.md` y `tasks.md` de F01, y de los equivalentes de F02, ya que ambos se aprueban juntos como parte de H1). La secuencia F01 ↔ F02 está detallada en `specs/F01-perfil-onboarding/plan.md` §1: la mayoría de las tareas de F01 y F02 corren en paralelo; solo `F01-T13`/`F01-T14` esperan a `F02-T10`.

| # | Estado | Tarea | Capa | Agente | CA cubiertos | Depende de |
|---|---|---|---|---|---|---|
| F01-T00 | ☑ | Aprobación del usuario de `plan.md` y `tasks.md` de F01 y F02 (H1 completo) | — | usuario | — | — |
| F01-T01 | ☑ | Pruebas de dominio para RN-01 (edad mínima 16 años), RN-02 (IMC) y RN-03 (TMB), incluyendo los valores de referencia de CA-01.05.1 | domain | qa-pruebas | CA-01.03.1, CA-01.05.1 | F01-T00 |
| F01-T02 | ☑ [P] | Implementar `shared/domain/{Gender,FitnessGoal,Level,Equipment,UnitSystem}.ts` (vocabulario compartido, ver plan §1) | domain | dev-mobile-rn | — | F01-T00 |
| F01-T03 | ☑ | Implementar `UserProfile`, `BodyMetric`, `Height` (VO con conversión de unidades) y `calculateAge`/`calculateBmi`/`calculateBmr` en `features/profile/domain/` | domain | dev-mobile-rn | CA-01.03.1, CA-01.03.2, CA-01.05.1 | F01-T01, F01-T02 |
| F01-T04 | ☑ [P] | Contract-first: agregar a `openapi.yaml` los endpoints `/auth/*`, `DELETE /me`, `/me/profile`, `/me/body-metrics`, `/sync/push`, `/sync/pull` (plan §3) y regenerar el cliente móvil (orval) | infra | dev-backend-java + dev-mobile-rn | — | F01-T00 |
| F01-T05 | ☑ | Backend módulo `identity`: dominio (`User`, `RefreshToken`, `PasswordResetToken`), puertos out, casos de uso (`RegisterUser`, `LoginUser`, `RefreshAccessToken`, `LogoutUser`, `RequestPasswordReset`, `ResetPassword`, `UpgradeGuestToAccount` vía `AuthController`, `DeleteAccount`), controladores (`AuthController`, `MeController`), persistencia JPA real, migración Flyway `identity` (Argon2id vía Spring Security Crypto + Bouncy Castle, JWT HS256 access 15 min / refresh 30 días rotativo con detección de reutilización). Nota: el envío real del correo de recuperación de contraseña queda fuera de alcance (no hay proveedor SMTP en el proyecto); el token se genera/persiste/valida igual, ver informe de la ronda | domain/application/infrastructure | dev-backend-java | CA-01.01.1, CA-01.07.1, CA-01.08.1 | F01-T04 |
| F01-T06 | ☑ | Backend módulo `profile`: dominio, puertos out, casos de uso (`GetProfile`, `UpsertProfile`, `AppendBodyMetric`, `GetBodyMetrics`), controlador `ProfileController` (`GET/PUT /me/profile`, `GET /me/body-metrics`), persistencia JPA real, migración Flyway `profile` | domain/application/infrastructure | dev-backend-java | RF-01.03, RF-01.06 | F01-T04 |
| F01-T07 | ☑ | Backend módulo `sync` mínimo: `SyncEntityHandler` SPI, `SyncController` (`POST /sync/push`/`GET /sync/pull`, cursor opaco), implementación del handler para `entity ∈ {profile, bodyMetric}` en `profile/adapters/out/sync` (LWW por `updatedAt`, RN-18/ADR-007), rechazo `UNSUPPORTED_ENTITY` para el resto | domain/application/infrastructure | dev-backend-java | CA-01.01.1, CA-01.06.1 | F01-T05, F01-T06 |
| F01-T08 | ☑ [P] | Móvil: `features/sync` mínimo — `OutboxEntry`, tabla `outbox`/`sync_cursor_state` (Drizzle), `PushPendingChanges`, `PullRemoteChanges`, `EnqueueChange`, `HttpSyncAdapter` (llama a `F01-T07`), `SqliteOutboxRepository`/`SqliteCursorStore`, `ProfileSyncEntityApplier` (SPI que `profile` implementa para `entity ∈ {profile, bodyMetric}`), cableado en `composition/container.ts` (suscribe `BodyWeightLogged`/`ProfileUpdated` y encola en el outbox) — hecho, con pruebas de aplicación (fakes) e infraestructura (SQLite real vía `sql.js`, MSW) | domain/application/infrastructure | dev-mobile-rn | CA-01.01.1, CA-01.06.1 | F01-T02, F01-T07 |
| F01-T09 | ☑ [P] | Móvil: esquema Drizzle `profiles`/`body_metrics` (+ `catalog_manifest_state`/`media_cache_entries`/`exercises`/`predefined_routines`/`routine_blocks`/`routine_items` de F02, mismo archivo compartido `shared/infrastructure/db/schema.ts`) + `SqliteProfileRepository`/`SqliteBodyMetricRepository` — hecho. Migraciones: bootstrap idempotente (`CREATE TABLE IF NOT EXISTS`) en `shared/infrastructure/db/migrate.ts` en vez del flujo oficial de `drizzle-kit`/`journal` (ver comentario del archivo: evita configurar Metro para empaquetar `.sql`, mismo efecto). Pruebas de integración con SQLite real vía `sql.js` (`test/helpers/createTestDb.ts`), no un doble en memoria hecho a mano | infrastructure | dev-mobile-rn | — | F01-T03 |
| F01-T10 | ☑ | Móvil: `HttpAuthAdapter` (register/login/guestUpgrade/deleteAccount sobre `apiClient`, mismo patrón que `HttpCatalogAdapter` en vez del cliente generado por orval — ver comentario del archivo) + `SecureTokenStorage` (sobre `shared/infrastructure/secure-storage/SecureStoreAdapter`, expo-secure-store) + `mutator.ts` ampliado para manejar respuestas sin cuerpo (202/204). **Desviación documentada**: no se implementó inyección global de `Authorization`/refresco en 401 en `mutator.ts` (cada adaptador inyecta su propio `Authorization` vía un `getAccessToken()` inyectado desde `composition/`, para no acoplar el mutator compartido —usado también por endpoints públicos— a la sesión); `refreshAccessToken` (rotación de refresh token) no tiene adaptador propio todavía porque `LoginUser`/`UpdateProfile` (F01-T12) tampoco están implementados en esta ronda. Probado con MSW y fakes de `expo-secure-store` | infrastructure | dev-mobile-rn | CA-01.01.1, CA-01.08.1 | F01-T04, F01-T05 |
| F01-T11 | ☑ | Casos de uso `CompleteOnboarding` (valida, persiste perfil + primer `BodyMetric`, calcula IMC/TMB) y `LogBodyWeight` (upsert por fecha) | application | dev-mobile-rn | CA-01.02.1, CA-01.03.1, CA-01.03.2, CA-01.04.1, CA-01.05.1, CA-01.06.1 | F01-T09, F01-T08 |
| F01-T12 | ☑ | Casos de uso `CreateAccount`, `ContinueAsGuest`, `LoginUser`, `UpdateProfile`, `DeleteAccount` — hecho: los cinco cableados con adaptadores reales en `composition/container.ts` (F01-T10/T13). `LoginUser` usa `HttpAuthAdapter.login`/`SecureTokenStorage` y traduce el `HTTP_401` genérico de `HttpAuthAdapter` (`mutator.ts` no propaga el cuerpo `Problem` todavía) al código real del contrato `AUTH_INVALID_CREDENTIALS` (CA-01.08.1, última línea). `UpdateProfile` usa el puerto nuevo `RemoteProfilePort` (`HttpProfileAdapter`, `PUT /me/profile`) y solo persiste localmente tras el update remoto exitoso; añadido también `GetCurrentProfile` (consulta de solo lectura para precargar "Editar perfil"). Pruebas propias con fakes/MSW para los tres casos de uso nuevos y el adaptador nuevo | application | dev-mobile-rn | CA-01.01.1, CA-01.02.1, CA-01.07.1, CA-01.08.1 | F01-T10, F01-T11 |
| F01-T13 | ☑ **[CRUZADA]** | Definir `RoutineProposalPort` en `profile/application/ports.ts` y cablearlo en `src/composition/container.ts` a `generateProposal` de `catalog` (import solo desde `composition/`, nunca desde `features/profile/**`); actualizar `dependency-cruiser` para prohibir `profile → catalog` fuera de `composition/` — hecho, incluido el cableado real en `container.ts` (`getAppContainer()`, memoizado, abre SQLite/secure-store una vez por proceso) y su prueba de integración (`src/composition/__tests__/container.test.ts`, ejercita `completeOnboarding` de punta a punta con SQLite real vía `sql.js`). `arch:check` confirma que `feature-isolation-*` sigue sin violaciones | application/infrastructure | dev-mobile-rn | — | F01-T12, **F02-T10** (ver `specs/F02-catalogo-propuesta/tasks.md`) |
| F01-T14 | ☑ | Pantallas del asistente de onboarding **salvo Resumen**: Bienvenida, Objetivo, Nivel, Disponibilidad, Equipo, Datos corporales, Cuestionario de aptitud, Consentimiento; `useOnboardingStore` (Zustand) — hecho, las 7 pantallas nuevas (`Welcome`, `Goal`, `Level`, `Availability`, `Equipment`, `BodyData`, `FitnessQuestionnaire`) más `Consent.tsx` (ya existente, integrada), orquestadas por `OnboardingNavigator` (`presentation/navigators/`, índice de paso en memoria, ver comentario del archivo sobre por qué no son 10 rutas de Expo Router) y `app/onboarding/index.tsx`. i18next real (`shared/i18n/index.ts`) sustituye el diccionario placeholder; `react-hook-form` + `zod` en `Availability`/`BodyData`/`AccountChoice`. Todas las pantallas tienen prueba RNTL propia + `OnboardingFlow.test.tsx` (integración de punta a punta) | presentation | dev-mobile-rn + ux-motivacion | CA-01.02.1, CA-01.03.1, CA-01.03.2, CA-01.04.1, CA-01.07.1 | F01-T11 |
| F01-T15 | ☑ **[CRUZADA]** | Pantalla "Resumen" (IMC/TMB + plan propuesto con aviso "Estimación, no es consejo médico") y elección Crear cuenta / Continuar como invitado, permiso de notificaciones en contexto — hecho: `Summary.tsx`, `AccountChoice.tsx`, `NotificationsPermission.tsx`, todas con prueba RNTL. **Desviación documentada**: `NotificationsPermission.tsx` no llama a una API real de permisos porque `expo-notifications` no estaba en el alcance de infraestructura de esta ronda (no listado en "Qué instalar" del encargo); deja los callbacks `onAllow`/`onSkip` como punto de extensión | presentation | dev-mobile-rn + ux-motivacion | CA-01.02.1, CA-01.05.1 | F01-T13, F01-T14 |
| F01-T16 | ☑ [P] | Pantallas de perfil: iniciar sesión, editar perfil, registrar peso, eliminar cuenta (confirmación escribiendo "ELIMINAR") — hecho: `Login.tsx`, `EditProfile.tsx`, `LogWeight.tsx`, `DeleteAccountConfirmation.tsx`, orquestadas por `ProfileNavigator` (`presentation/navigators/`, mismo patrón de índice de vista en memoria que `OnboardingNavigator`/`CatalogNavigator`) y `app/profile/index.tsx`. `EditProfile` solo edita objetivo/nivel/disponibilidad/estatura/equipo/peso objetivo (no `birthDate`/`gender`/`unitSystem`/consentimiento, ver comentario de `UpdateProfile.ts`). Todas las pantallas tienen prueba RNTL propia + `ProfileNavigator.test.tsx` (integración) | presentation | dev-mobile-rn + ux-motivacion | CA-01.06.1, CA-01.08.1 | F01-T12 |
| F01-T17 | ☑ | Revisión de seguridad y privacidad: JWT (rotación/revocación de refresh), Argon2id, consentimiento de datos de salud (Art. 5.3), eliminación de cuenta (datos inaccesibles después, RN backend). Encontró 2 hallazgos bloqueantes: **C1** (DELETE /me no purgaba `profile_profiles`/`profile_body_metrics`, violaba Art. 5.4/RNF-08) y **A1** (condición de carrera en rotación de refresh tokens permitía eludir detección de reuso). Ambos corregidos: C1 vía evento de dominio `AccountDeleted` (identity→profile, síncrono en la misma transacción) + `PurgeProfileData`; A1 vía `UPDATE ... WHERE revoked_at IS NULL` atómico. `./gradlew check` verde tras el fix. M1-M4/B1-B2 quedan como deuda técnica no bloqueante (rate limit sin purga, JWT secret sin fail-fast, falta logout en móvil, sync/push sin límite de tamaño, copy de notificaciones, tokens de reset no invalidados al borrar cuenta) | — | seguridad-privacidad | CA-01.07.1, CA-01.08.1 | F01-T05, F01-T07, F01-T10 |
| F01-T18 | ☑ | Verificación final y matriz de trazabilidad CA-01.01.1 … CA-01.08.1 contra `plan.md` §5 — 9/9 CA con prueba que los cita y pasa (móvil 90 suites/388 tests, backend 122 tests). Alcance parcial de CA-01.01.1 confirmado (perfil + `bodyMetric`, sin rutinas/sesiones). ADR-002/004/007/008 promovidos a Aceptado. Deuda no bloqueante: pruebas Testcontainers de integración JPA no ejecutables en esta máquina Windows (incompatibilidad Docker Desktop conocida), pendientes de confirmar en CI Linux | — | qa-pruebas | todos | F01-T15, F01-T16, F01-T17 |

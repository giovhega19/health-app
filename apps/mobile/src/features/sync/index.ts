/**
 * API publica del modulo "sync".
 *
 * Corresponde a la sincronizacion offline-first (outbox + motor de sync); no tiene un Fxx dedicado, se implementa de forma incremental desde el plan tecnico de F01 en adelante. Ningun otro modulo debe importar los
 * internos de "sync" (domain/, application/, infrastructure/,
 * presentation/): solo este archivo (Art. 2.5 de la constitucion).
 *
 * Placeholder fundacional (H0): sin logica de negocio todavia. Se
 * completa en el plan tecnico correspondiente.
 */
export {};

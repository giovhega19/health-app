/**
 * Generated by orval 🍺
 * Do not edit manually.
 * FitApp API
 * Contrato de la API de FitApp (contract-first). Fuente de verdad para el backend (Spring Boot) y el cliente TypeScript generado que consume la app móvil (Expo). En H0 este archivo es un stub sin endpoints: los endpoints de cada módulo (identity, profile, catalog, sync, ...) se agregan de forma incremental en el plan técnico de cada Fxx, según ADR-004.
 * OpenAPI spec version: 1.0.0
 */
import type { ProblemFieldError } from './problemFieldError';

/**
 * Formato de error RFC 9457 (application/problem+json) usado en todas las respuestas de error de la API (ver 06-contratos-api.md §1).
 */
export interface Problem {
  /** URI que identifica el tipo de problema. */
  type: string;
  /** Resumen legible y corto del tipo de problema. */
  title: string;
  /** Código de estado HTTP. */
  status: number;
  /** Explicación específica de esta ocurrencia del problema. */
  detail?: string;
  /** Código de error estable y legible por máquina. */
  code: string;
  /** Errores de validación por campo, cuando aplica. */
  errors?: ProblemFieldError[];
}

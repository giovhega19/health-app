/**
 * Generated by orval 🍺
 * Do not edit manually.
 * FitApp API
 * Contrato de la API de FitApp (contract-first). Fuente de verdad para el backend (Spring Boot) y el cliente TypeScript generado que consume la app móvil (Expo). Los endpoints de cada módulo (identity, profile, catalog, sync, ...) se agregan de forma incremental en el plan técnico de cada Fxx, según ADR-004. En H1 se agregan los endpoints de `identity`, `profile`, `sync` (mínimo, solo `profile`/`bodyMetric`) y `catalog` (solo lectura), según `specs/F01-perfil-onboarding/plan.md` y `specs/F02-catalogo-propuesta/plan.md`.
 * OpenAPI spec version: 1.0.0
 */
import type { Equipment } from './equipment';
import type { ExerciseMode } from './exerciseMode';

/**
 * 05-modelo-dominio-reglas.md §1 (class Exercise).
 */
export interface ExerciseDto {
  id: string;
  slug: string;
  name: string;
  /** Grupos musculares principales trabajados. Taxonomía aún no fijada como enum cerrado en 05-modelo-dominio-reglas.md; se modela como texto libre para no anticipar valores no aprobados. */
  muscleGroups: string[];
  equipment: Equipment[];
  /**
     * Escala 1 (más fácil) a 3 (más difícil), usada por RN-14.
     * @minimum 1
     * @maximum 3
     */
  difficulty: number;
  mode: ExerciseMode;
  /** Equivalente metabólico, usado en RN-04. */
  met: number;
  /** @minItems 3 */
  instructions: string[];
  /** @minItems 2 */
  commonMistakes: string[];
  imageUrl: string;
  /** @nullable */
  animationUrl?: string | null;
  /** @nullable */
  videoUrl?: string | null;
  updatedAt: string;
}

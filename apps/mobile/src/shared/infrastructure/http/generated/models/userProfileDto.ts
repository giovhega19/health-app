/**
 * Generated by orval 🍺
 * Do not edit manually.
 * FitApp API
 * Contrato de la API de FitApp (contract-first). Fuente de verdad para el backend (Spring Boot) y el cliente TypeScript generado que consume la app móvil (Expo). Los endpoints de cada módulo (identity, profile, catalog, sync, ...) se agregan de forma incremental en el plan técnico de cada Fxx, según ADR-004. En H1 se agregan los endpoints de `identity`, `profile`, `sync` (mínimo, solo `profile`/`bodyMetric`) y `catalog` (solo lectura), según `specs/F01-perfil-onboarding/plan.md` y `specs/F02-catalogo-propuesta/plan.md`.
 * OpenAPI spec version: 1.0.0
 */
import type { Equipment } from './equipment';
import type { FitnessGoal } from './fitnessGoal';
import type { Gender } from './gender';
import type { Level } from './level';
import type { UnitSystem } from './unitSystem';

/**
 * Réplica server-side minimalista del perfil (05-modelo-dominio-reglas.md §1). El servidor no recalcula IMC/TMB: esos cálculos son solo de UI del cliente (RN-02, RN-03).
 */
export interface UserProfileDto {
  id: string;
  birthDate: string;
  gender: Gender;
  /**
     * @minimum 100
     * @maximum 250
     */
  heightCm: number;
  goal: FitnessGoal;
  level: Level;
  /**
     * @minimum 1
     * @maximum 7
     */
  daysPerWeek: number;
  /**
     * @minimum 10
     * @maximum 120
     */
  minutesPerSession: number;
  equipment: Equipment[];
  unitSystem: UnitSystem;
  /**
     * @minimum 25
     * @maximum 350
     * @nullable
     */
  targetWeightKg?: number | null;
  /** `true` si el cuestionario de aptitud dio positivo (RN-14 solo propone BEGINNER/GENERAL_HEALTH mientras siga en `true`). */
  parqFlagged: boolean;
  healthConsentAt: string;
  updatedAt: string;
}

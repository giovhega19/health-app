/**
 * Carpeta reservada para el adaptador real del puerto `Clock`
 * (`shared/domain/Clock.ts`) basado en `Date` del sistema. Sin
 * implementación todavía: se agrega junto con la primera feature que
 * necesite el reloj (p. ej. `workout-session`, F05).
 */
export {};

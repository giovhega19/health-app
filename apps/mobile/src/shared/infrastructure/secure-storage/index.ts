/**
 * Adaptador de almacenamiento seguro (expo-secure-store, ver
 * 04-arquitectura.md §2), usado por `features/profile/infrastructure/SecureTokenStorage.ts`
 * para guardar los tokens de autenticación (identity/F01, Art. 5).
 */
export { SecureStoreAdapter } from "./SecureStoreAdapter";

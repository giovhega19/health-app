{
  "name": "@fitapp/api-contract",
  "private": true,
  "version": "1.0.0",
  "description": "Fuente de verdad del contrato de la API de FitApp (OpenAPI 3.1, contract-first).",
  "scripts": {
    "lint": "redocly lint openapi.yaml",
    "generate": "orval --config orval.config.ts",
    "validate": "pnpm run lint && pnpm run generate && git diff --exit-code -- ."
  },
  "devDependencies": {
    "@redocly/cli": "2.54.2",
    "orval": "8.36.0"
  }
}
